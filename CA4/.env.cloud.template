# CA4 Cloud Environment Variables Template
# Copy this file to .env.cloud and replace with your actual credentials
# NEVER commit .env.cloud to version control

# InfluxDB Configuration
INFLUXDB_PASSWORD=REPLACE_WITH_SECURE_PASSWORD
INFLUXDB_TOKEN=REPLACE_WITH_SECURE_TOKEN

# Grafana Configuration
GRAFANA_PASSWORD=REPLACE_WITH_SECURE_PASSWORD

# Generate secure values using:
# - Password: openssl rand -base64 16
# - Token: openssl rand -base64 32
